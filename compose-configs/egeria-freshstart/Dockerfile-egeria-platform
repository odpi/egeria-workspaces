FROM quay.io/odpi/egeria-platform:latest

# Replace any base-image sample secrets with the freshstart-specific set only.
RUN rm -rf /deployments/loading-bay/secrets \
	&& mkdir -p /deployments/loading-bay/secrets

COPY secrets/egeria-user-directory.omsecrets /deployments/loading-bay/secrets/
COPY secrets/egeria-servers.omsecrets /deployments/loading-bay/secrets/
COPY secrets/integration.omsecrets /deployments/loading-bay/secrets/

