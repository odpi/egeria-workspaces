fastapi
uvicorn[standard] # uvicorn[standard] includes httptools and websockets for better performance
pydantic
requests
httpx

pyegeria>=6.0.17.8  # NOT YET PUBLISHED to PyPI as of 2026-07-31 -- this pin will fail a fresh `pip install` until it is (see egeria-python pyproject.toml, commit 97d55fc). 6.0.14.4/.5 break get_notes_for_note_log (Note Logs tab); 6.0.16.16 adds External Identifier support (find/get_external_identifier(s), ExternalIdLink); 6.0.17.5 adds support for AI-ready data metrics and Vega-Lite chart generators; 6.0.17.8 adds typed exception classes + auto-renew expired bearer tokens, fixes get_collection_members dropping non-Collection members, adds the "Report Parameters" Dr.Egeria attribute, fixes the TABLE-format wikilinks leak, and moves Create Dashboard Sheet from "Dashboard Sheet Name" to "Display Name"
nest_asyncio
email-validator
loguru
rich
click
mcp >=1.15.0

# Demo-mode dependencies
sqlalchemy
bcrypt
python-jose[cryptography]
python-multipart
slowapi
apscheduler
docker
resend