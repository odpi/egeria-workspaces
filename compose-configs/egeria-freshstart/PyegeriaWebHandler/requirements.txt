fastapi
uvicorn[standard] # uvicorn[standard] includes httptools and websockets for better performance
pydantic
requests
httpx

pyegeria>=6.1.7  # 6.0.14.4/.5 break get_notes_for_note_log (Note Logs tab); 6.0.16.16 adds External Identifier support (find/get_external_identifier(s), ExternalIdLink); 6.0.17.5 adds support for AI-ready data metrics and Vega-Lite chart generators; 6.0.17.8 adds typed exception classes + auto-renew expired bearer tokens, fixes get_collection_members dropping non-Collection members, adds the "Report Parameters" Dr.Egeria attribute, fixes the TABLE-format wikilinks leak, and moves Create Dashboard Sheet from "Dashboard Sheet Name" to "Display Name"; 6.0.17.15 fixes MetadataExpert.find_metadata_elements silently dropping start_from/page_size/graph_query_depth (now sends startFrom/pageSize as URL query params, graphQueryDepth in the body) -- NOTE: confirmed live 2026-08-05 that Egeria's own server still ignores startFrom/pageSize on this endpoint regardless (ISSUE tracked in this repo's PYEGERIA_ISSUES.md) -- insights_handler.py's client-side GUID-dedup workaround is still required, this pyegeria fix alone does not restore real pagination; 6.0.18.2 ships the Overview dashboard's karma_leaderboard/engagement_series/orphan_glossary_terms/stale_assets/contextualised_coverage/count_elements_by_property functions (all registered in analytic_registry.py) and the new "Link Governance Results" Dr.Egeria command (PYEGERIA_ISSUES.md ISSUE-61) for attaching a GovernanceMetric to its implementation; 6.1.0 (2026-08-22) is a major release -- full audited client coverage of all 40 Egeria view services (44 omvs modules), Dr.Egeria's dispatcher is now a two-pass interpreter so forward references between elements in the same markdown file resolve instead of erroring, glossary term membership sync is ~35-40x faster, DeleteElementRequestBody's cascade_delete/delete_method fields actually get sent now (were silently dropped, ISSUE-62), and the version scheme moves off the old 6.0.18.x 4-part numbering to plain semver; 6.1.1 adds the missing Advanced template for Link Schema Attribute Definition.
nest_asyncio
email-validator
loguru
rich
click
mcp >=2.1.1  # This directory's own mcp_server.py (source-fixed 2026-07-31: FastMCP->mcp.server.mcpserver.MCPServer, dropped the dead server.settings.transport_security assignment) is the only mcp server this container actually imports/runs (pyegeria_handler.py:198 does `from mcp_server import server`) -- confirmed pyegeria's own pyegeria/core/mcp_server.py is NOT imported anywhere in this container (no reference to pyegeria.core.mcp_server or the `pyegeria-mcp` console script here), so this pin is independent of whether pyegeria has published its own mcp_server.py fix yet. Previously left at <2.0.0 out of caution for that unrelated file, which just meant this container's own already-migrated code had no matching mcp installed -- ModuleNotFoundError: No module named 'mcp.server.mcpserver'. If this container ever starts importing pyegeria.core.mcp_server directly, re-check that it's compatible with mcp>=2.0.0 (i.e. a pyegeria release containing the mcp_server.py fix) before relying on this pin.

# Demo-mode dependencies
sqlalchemy
bcrypt
python-jose[cryptography]
python-multipart
slowapi
apscheduler
docker
resend