services:
  egeria-main:
    extra_hosts:
      - "${HOST_FQDN}:${HOST_GATEWAY_IP}"
      - "host.docker.internal:${HOST_GATEWAY_IP}"

  pyegeria-web:
    extra_hosts:
      - "${HOST_FQDN}:${HOST_GATEWAY_IP}"
      - "host.docker.internal:${HOST_GATEWAY_IP}"

  jupyter-hub:
    extra_hosts:
      - "${HOST_FQDN}:${HOST_GATEWAY_IP}"
      - "host.docker.internal:${HOST_GATEWAY_IP}"
