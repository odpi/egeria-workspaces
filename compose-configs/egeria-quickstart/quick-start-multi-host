#!/usr/bin/env bash
set -euo pipefail

echo "[DEPRECATION] This script has moved to the repo root as './quick-start-multi-host'."
echo "Invoking the new location..."

REPO_ROOT_DIR="$(cd "$(dirname "$0")"/../.. && pwd)"
exec "${REPO_ROOT_DIR}/quick-start-multi-host" "$@"
