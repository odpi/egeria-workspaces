[pytest]
# Establishes this directory as pytest's confcutdir so it doesn't walk up and
# collect ../conftest.py (which imports fastapi — not installed in this
# tier's venv; see requirements.txt for why this tier has its own env).
