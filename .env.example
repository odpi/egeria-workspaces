# ─────────────────────────────────────────────────────────────────────────────
# egeria-workspaces root — environment configuration template
# This file is for the root-level compose (if used directly).
# Most users should use the .env.example in the relevant compose-configs/ subfolder.
# Copy this file to .env and fill in values for your environment.
# ─────────────────────────────────────────────────────────────────────────────

HOST_FQDN=myhost.local
KAFKA_CLUSTER_ID=42
KAFKA_BOOTSTRAP_SERVERS=myhost.local:9194
CONFIG_JSON=""
